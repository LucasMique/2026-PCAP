# OS SETES PILARES DA PROGRAMAÇÃO NO PROGRAMA ADVINHE O NÚMERO:

# ENTRADA:
1- palpite = int(input("Seu palpite (1 a " + str(maximo) + "): "))
2- opcao = int(input("Digite 1, 2, ou 3:"))

# Saída:
1 - print("🎊 Acertou!")
2 - print("📈 Muito baixo!")
3 - print("📉 Muito alto!")

# Operadores:
1 - nivel = niveis[opcao - 1]
2 - elif palpite < numero_secreto:
3 - while chances > 0 and not acertou:

# Sub-rotinas:
1 - def jogar(maximo, chances)
2 - import random


# Condição:
1 - if palpite == numero_secreto:
2 - elif palpite < numero_secreto:
3 - else:

# Repetição:
1 - while chances > 0 and not acertou:


# Variáveis:
1 - nivel = niveis[opcao - 1]
2 -  numero_secreto = random.randint(1, maximo)
3 - aresenhanaopara = 999